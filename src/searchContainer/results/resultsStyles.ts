import { StyleSheet } from "types/AppTypes";

const styles: StyleSheet = {
  container: {
    listStyleType: "none",
    margin: 0,
    padding: 0,
    display: "flex",
    flexDirection: "row",
    width: "100%",
  },
};

export default styles;
